services:
  - type: web
    name: hypertension-prediction
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app6.py --server.port $PORT --server.address 0.0.0.0
    plan: free